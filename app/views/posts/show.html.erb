<h1><%= @post.title %></h1>
<p><%= @post.text %></p>
<% @post.comments.each do |comment| %>
  <%= render 'comments/comment', comment: comment %>
<% end %>
<% if logged_in? %>
  <%= render 'comments/form', post: @post %>
<% end %>
