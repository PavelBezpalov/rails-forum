<div class='comment-submit-form'>
  <%= form_for(:comment, url: post_comments_path(post)) do |f| %>
    <div class="form-group">
      <%= f.label :text, 'Enter your comment' %>
      <%= f.text_area :text %>
    </div>
    <%= f.submit 'Comment' %>
  <% end %>
</div>
